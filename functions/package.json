{
  "name": "warsworld-render-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint --ext .ts src"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.6.0",
    "gifenc": "^1.0.3",
    "sharp": "^0.34.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.10",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
